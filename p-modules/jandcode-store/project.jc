import jandcode.jc.*
import jandcode.jc.std.*

class P extends ProjectScript {

    static beforeLoad = {
    }

    void onInclude() {
        //
        project.name = "jandcode-store"
        //
        include(JavaProject).with {
            moduleDef("jandcode.store").with {
                depends.prod.add(
                )
                depends.dev.add(
                )
            }
        }
    }

}