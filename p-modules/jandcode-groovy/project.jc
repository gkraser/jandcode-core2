import jandcode.jc.*
import jandcode.jc.std.*

class P extends ProjectScript {

    static beforeLoad = {
    }

    void onInclude() {
        //
        project.name = "jandcode-groovy"
        //
        include(JavaProject).with {
            moduleDef("jandcode.groovy").with {
                depends.prod.add(
                )
            }
            //
            depends.prod.add(
            )
            depends.dev.add(
            )
            //
        }
    }

}