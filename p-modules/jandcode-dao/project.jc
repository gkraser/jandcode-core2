import jandcode.jc.*
import jandcode.jc.std.*

class P extends ProjectScript {

    static beforeLoad = {
    }

    void onInclude() {
        //
        project.name = "jandcode-dao"
        //
        include(JavaProject).with {
            moduleDef("jandcode.dao").with {
                depends.prod.add(
                )
            }
            //
            depends.prod.add(
                    'javassist'
            )
            depends.dev.add(
            )
            //
        }
    }

}