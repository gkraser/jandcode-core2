import jandcode.jc.*
import jandcode.jc.std.*

class P extends ProjectScript {

    static beforeLoad = {
    }

    void onInclude() {
        //
        project.name = "jandcode-db-postgresql"
        //
        include(JavaProject).with {
            moduleDef("jandcode.db.postgresql").with {
                depends.prod.add(
                        "jandcode.db",
                        "postgresql",
                )
                depends.dev.add(
                )
            }
        }
    }

}