import jandcode.jc.*
import jandcode.jc.std.*

class P extends ProjectScript {

    void onInclude() {
        //
        project.name = "jandcode-db-oracle"
        //
        include(JavaProject).with {
            moduleDef("jandcode.db.oracle").with {
                depends.prod.add(
                        "jandcode.db",
                )
            }
            //
            depends.prod.add(
                    // "jdbc-oracle", // внешняя зависимость
            )
            depends.dev.add(
            )
            //
        }
    }

}