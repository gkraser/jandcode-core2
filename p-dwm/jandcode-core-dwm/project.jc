import jandcode.jc.*
import jandcode.jc.std.*

class P extends ProjectScript {

    static beforeLoad = {
    }

    void onInclude() {
        //
        project.name = "jandcode-core-dwm"
        //
        include(JavaProject).with {
            moduleDef("jandcode.core.dwm").with {
                depends.prod(
                )
                depends.dev(
                )
            }
        }
    }

}