/* Тема base
----------------------------------------------------------------------------- */
import css from './std-theme.scss'

let config = require('[*]/css/std-config.js')

export default {
    css: css,
    config: config
}