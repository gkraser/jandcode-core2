package jandcode.xcore.std;

import jandcode.xcore.*;

/**
 * Сервис для поверки изменных ресурсов приложения (conf, загруженных в приложение).
 * Это внутренний сервис, в приложении напрямую не используется.
 */
public interface AppCheckChangedResourceService extends Comp, ICheckChangedResource {
}
